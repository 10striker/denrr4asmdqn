<!DOCTYPE html>
<html lang="tl">
<head>
  <meta charset="UTF-8" />
  <title>Login</title>
  <style>
    body {
      font-family: sans-serif;
      display: flex;
      justify-content: flex-start; /* ilipat sa kaliwa */
      align-items: center;
      height: 4vh;
      background: #d3e3fd;
      padding-left: 50px; /* optional: space mula sa gilid */
    }
    form {
      background: white;
      padding: 50px;
      border-radius: 20px;
      box-shadow: 0 0 10px rgba(0,0,0,0.3);
      width: 300px;
    }
    input {
      display: block;
      margin: 10px 0;
      padding: 10px;
      width: 100%;
    }
    button {
      padding: 10px 15px;
      background: #007bff;
      color: white;
      border: none;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <form method="POST" action="/login" autocomplete="off">
    <h2>Admin Login</h2>
    <input name="username" placeholder="Username" required autocomplete="off" />
    <input name="password" type="password" placeholder="Password" required autocomplete="off" />
    <button type="submit">Login</button>
  </form>
</body>
</html>
